spring:
  profiles:
    active: dev  #dev para h2 e docker para postgreSQL
  jackson:
    deserialization:
      fail-on-unknown-properties: false

logging:
  level:
    com.jhops10.musicrequestapi: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE